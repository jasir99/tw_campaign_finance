name: tw_campaign_finance
version: 1.0.0
config-version: 2
profile: tw_campaign_finance
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
target-path: target
clean-targets:
  - target
  - dbt_packages
models:
  tw_campaign_finance:
    +tags: piperider
    example:
      +materialized: view