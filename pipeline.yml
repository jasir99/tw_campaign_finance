name: "CI"
jobs:
  - name: "run"
    steps:
      - name: dbt run (build models)
        plugin: dbt@v1
        command: dbt run
        working_dir: dbt
 
      # Step 5: dbt tests the data
      - name: dbt test
        plugin: dbt@v1
        command: dbt test
        working_dir: dbt